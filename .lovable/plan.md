## Add "high-level engineer" description to About section

### Scope
Add a short paragraph (2–3 sentences) to the **About** section in `src/components/Portfolio.tsx` that describes the user as a "high-level engineer" — someone who avoids low-level implementation (e.g. vLLM internals) but has broad knowledge across the LLM/HPC stack and connects the pieces together.

### Steps
1. Draft 2–3 phrasing options for the new paragraph.
2. User picks one.
3. Insert the chosen paragraph into the About section, between the existing two paragraphs.
4. Verify the page renders correctly in the preview.

### No other changes
- No new sections, no new pages, no dependency installs.
- Only modifies `src/components/Portfolio.tsx`.