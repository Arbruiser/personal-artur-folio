## Change: Replace hero tagline

Update the single-sentence tagline under `~/portfolio $ whoami` in `src/components/Portfolio.tsx`.

**Current:**
> I help users get the most out of large language models on Europe's LUMI supercomputer.

**Proposed:**
> I work across the full LLM stack at the LUMI AI Factory — from fine-tuning and evaluation to production inference on Europe's fastest supercomputer.

**Why this fits:**
- Punchy & confident tone
- Mentions the LUMI AI Factory identity
- Signals breadth (fine-tuning → evaluation → inference)
- Replaces "help users get the most out of" with active, specific verbs
- Keeps the "Europe's fastest supercomputer" anchor

**Scope:** One-line string change in the `<p>` inside the page header. No other files touched.